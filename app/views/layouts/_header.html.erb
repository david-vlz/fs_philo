		<header class="center page-header">
			<h1>Fachschaft Philosophie <small>Universit&auml;t zu K&ouml;ln</small></h1>
		</header>
<!--		<div class="row-fluid">-->
<!--			<div class="span2">-->
				<!-- no content here yet, logo? -->
<!--			</div>-->
<!--			<div class="span8"-->
				<div class="navbar">
					<div class="navbar-inner">
						<ul class="nav nav-pills">
						
							<li><%= link_to("Home", root_path) %></li>
							
							<% fetchCategories.each do |category| %>
								<% if category.visible? %>
									<li><%= link_to(category.name, category_path(category)) %></li>
								<% elsif signed_in? %>
									<li><%= link_to(category.name, category_path(category), class: 'muted') %></li>
								<% end %>
							<% end %>
							
							<% if signed_in? %>
								<li><%= link_to('+', new_category_path, title: 'Neue Seite anlegen') %></li>
							<% end %>
							
						</ul>
						<ul class="nav nav-pills pull-right">
						
							<% if signed_in? %>
								<li><%= link_to('Users', users_path) %></li>
								<li><%= link_to('Profil', current_user) %></li>
								<li><%= link_to('Logout', logout_path, method: "delete") %></li>
							<% else %>
								<li><%= link_to('Login', login_path) %></li>
							<% end %>
							
						</ul>
					</div>
				</div>
<!--			</div>-->
<!--		</div>-->
