<% provide(:title, "#{@article.title} - Versionen") %>
<h3>Fr&uuml;here Versionen von"<%= @article.title  %>"</h3>
<section>

	<table class="table table-striped table-condensed">
		<thead>
			<tr>
				<th>id</th><th>Aktion</th><th>ge&auml;ndert durch</th><th>Datum</th><th><!--empty header for actions row--></th>
			</tr>
		</thead>
		<tbody>
		<% @versions.each do |version| %>
			<% if version.object != nil %>
				<tr>
					<td><%= version.id %></td>
					<td><%= version.event %></td>
					<td>
						<% @who = version.whodunnit || '?' %>
						<% @user = User.find_by_id(@who.to_i) if '?' != @who %>
						<%= @user ? @user.name : @who %>
					</td>
					<td><%= version.created_at %></td>
					<td>

					</td>
				</tr>
			<% end %>
		<% end %>
		</tbody>
	</table>
	
</section>
